// The blog's own `openGraph` metadata replaces the root's file-based image, so the index needs its own file.
// Posts have theirs in [slug]/opengraph-image.tsx.
export { default, alt, size, contentType } from "../opengraph-image"
